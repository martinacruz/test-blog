<%- include('partials/header')-%>

<h1>Home</h1>
<p><%= innerText%></p>

<% usersPosts.forEach((post) => { %>
    <h3><%= post.title%></h3>
    <p><%= post.content%></p>
    <a href="/post/<%= post.title %>" value=<%= post %>>Read More...</a>
 <% }) %>
    

<%- include('partials/footer')-%> 