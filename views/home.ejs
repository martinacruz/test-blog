<%- include('partials/header')-%>

<h1>Home</h1>
<p><%= innerText%></p>

<% usersPosts.forEach((post) => { %>
    <h3><%= post.title%></h3>
    <% if (post.content.length > 100) { %>
        <p><%= post.content.substring(0, 100)%> ...    <a href="/post/<%= post.title %>" value=<%= post %>>Read More...</a>
        </p>
        <% } else { %>

        <p><%= post.content%></p>
        <% } %>
    <a href="/post/<%= post.title %>" value=<%= post %>>Read More...</a>
 <% }) %>
    

<%- include('partials/footer')-%> 